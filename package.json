{
  "private": true,
  "name": "worker",
  "version": "1.0.0",
  "description": "This worker is used to return IP addres, country and ASN of those in Singpoare. If not in singapore they will be redirect to https://1.1.1.1/",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Joel Fernandez <joel.fernandez@live.com.au>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
